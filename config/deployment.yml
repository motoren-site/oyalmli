apiVersion: apps/v1
kind: Deployment
metadata:
  name: oyalmli
  labels:
    app: oyalmli
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oyalmli
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: oyalmli
    spec:
      containers:
      - name: oyalmli
        image: <IMAGE>
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 100m
          limits:
            cpu: 100m
---
apiVersion: v1
kind: Service
metadata:
  name: oyalmli-service
spec:
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: oyalmli