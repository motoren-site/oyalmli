apiVersion: apps/v1
kind: Deployment
metadata:
  name: oyalmli
  labels:
    app: oyalmli
spec:
  replicas: 3
  selector:
    matchLabels:
      app: oyalmli
  template:
    metadata:
      labels:
        app: oyalmli
    spec:
      containers:
      - name: oyalmli
        image: registry.digitalocean.com/olsen/oyalmli
        ports:
        - containerPort: 80